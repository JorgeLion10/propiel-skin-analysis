// This component is no longer used and has been replaced by BottomNavBar.
// It can be deleted from the project.
import React from 'react';

const Header: React.FC = () => {
  return null; 
};

export default Header;