import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cl.propiel.propielskinanalysis',
  appName: 'Propiel Skin Analysis',
  webDir: 'dist'
};

export default config;
