import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cl.propiel.propielskinanalysis',
  appName: 'propiel---ai-skin-analysis',
  webDir: 'dist'
};

export default config;
